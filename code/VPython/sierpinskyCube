from visual import *

def makeSierpinskiCube( pos, length, height, width, level):
    if level = 1:
        box(pos, length, height, width, level -= 1)
    else:
        newLength = length / 3
        newHeight = height / 3
        newWidth = width / 3

        
